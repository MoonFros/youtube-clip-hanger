# Optional AI extras — not required, the app degrades gracefully without them.
#
#   pip install -r backend/requirements-ai.txt
#
# faster-whisper powers fully offline word-level transcription. It downloads a
# model (~150 MB for "base") the first time a job needs it; set
# FAIRCLIP_WHISPER_MODEL=small for better accuracy or =tiny for speed.
# Without it you can still paste a transcript in the editor, or set
# OPENAI_API_KEY to use the Whisper API.
faster-whisper>=1.0
