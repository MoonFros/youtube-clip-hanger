# Windows fallback — same runtime as requirements.txt, but pinned to versions
# that are guaranteed to have prebuilt win_amd64 wheels (no C++ build tools).
# Use it only if `pip install -r requirements.txt` fails on Windows:
#
#   python -m venv backend\.venv
#   backend\.venv\Scripts\python.exe -m pip install --only-binary=:all: -r backend\requirements-windows.txt
#
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
numpy>=1.26,<3
av>=12.1,<13
pillow>=10.0
httpx>=0.27
yt-dlp>=2024.4.9
